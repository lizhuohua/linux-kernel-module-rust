[package]
name = "simple_sysctl"
version = "0.1.0"
authors = ["Li Zhuohua <lizhuohua1994@gmail.com>"]
edition = "2018"

[lib]
crate-type = ["staticlib"]

[dependencies]
linux-device-driver = { path = ".." }

[profile.release]
panic = "abort"
lto = true

[profile.dev]
panic = "abort"
